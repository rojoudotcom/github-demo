{
  "name": "github-demo",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "GitHub Flow（ブランチ・PR・CI・マージ・GitHub Pages への公開）を実演する、素の HTML/CSS/JS で作ったタスクボード。",
  "scripts": {
    "serve": "node scripts/serve.mjs",
    "test": "node --test test/unit/*.test.js",
    "lint": "eslint . && prettier --check .",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "eslint": "^9.13.0",
    "prettier": "^3.3.3"
  }
}
